name: Build main documentation

on:
  push:
    branches:
      - main
      - doc-builder*
      - v*-release

jobs:
  build_documentation:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}

    steps:
      - uses: actions/checkout@v2
        with:
          repository: 'huggingface/doc-builder'
          path: doc-builder

      - uses: actions/checkout@v2
        with:
          repository: 'huggingface/doc-build'
          path: doc-build
          token: ${{ secrets.HUGGINGFACE_PUSH }}

      - uses: actions/checkout@v2
        with:
          repository: 'huggingface/optimum-habana'
          path: optimum-habana

      - name: Set environment variables
        run: |
          cd optimum-habana
          version=`echo "$(grep '^__version__ =' optimum/habana/version.py | cut -d '=' -f 2- | xargs)"`

          if [[ $version == *.dev0 ]]
          then
            echo "VERSION=main" >> $GITHUB_ENV
          else
            echo "VERSION=v$version" >> $GITHUB_ENV
          fi

          cd ..

      - name: Setup environment
        run: |
          pip uninstall -y doc-builder
          cd doc-builder
          git pull origin main
          pip install .
          pip install black
          cd ..

      - name: Create build directory
        run: |
          mkdir -p build_dir/optimum-habana
          cd build_dir/optimum-habana
          wget https://raw.githubusercontent.com/huggingface/doc-build/main/optimum-habana/_versions.yml

      - name: Make documentation
        run: |
          cd optimum-habana
          make doc BUILD_DIR=habana-doc-build VERSION=${{ env.VERSION }}
          sudo cp -r habana-doc-build/optimum.habana/${{ env.VERSION }} ../build_dir/optimum-habana
          cd ..

      - name: Push to repositories
        run: |
          cd build_dir
          doc-builder push optimum-habana --doc_build_repo_id "huggingface/doc-build" --token ${{ secrets.HUGGINGFACE_PUSH }} --commit_msg "Updated with commit ${{ github.sha }} See: https://github.com/huggingface/optimum-habana/commit/${{ github.sha }}" --n_retries 5
        shell: bash
